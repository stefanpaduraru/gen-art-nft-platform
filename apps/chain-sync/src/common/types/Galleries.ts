export type MintoriaGallery = 'selected' | 'open-world';
export enum MintoriaGalleries {
  Selected = 'selected',
  OpenWorld = 'open-world',
}

export enum MintoriaGalleryTitles {
  Selected = 'Selected',
  OpenWorld = 'Open World',
}
